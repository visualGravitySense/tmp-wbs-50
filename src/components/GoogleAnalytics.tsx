'use client'

/**
 * Google Analytics loader — currently disabled site-wide.
 * See ANALYTICS_ENABLED / GA_MEASUREMENT_ID in @/lib/analytics.
 * Kept as a no-op so any future import does not inject gtag scripts.
 */
export default function GoogleAnalytics() {
  return null
}
